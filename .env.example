# Copy to .env and fill in — never commit the real file.
# .env is already in .gitignore.

# Required for demo mode and the standalone watchdog
ANTHROPIC_API_KEY=sk-ant-...

# Optional — enables real-time alerts from monitor/watchdog.py
DISCORD_WEBHOOK_URL=
SLACK_WEBHOOK_URL=
PAGERDUTY_ROUTING_KEY=
